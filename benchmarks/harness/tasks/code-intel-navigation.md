# Benchmark Task: Code Intelligence Navigation

Prove native code-intelligence primitives can locate, outline, pack, and brief relevant repo context faster than ad-hoc shell exploration.

Command:

```bash
npm run smoke:agent-tooling
```
