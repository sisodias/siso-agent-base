# Repo Index

Goal: validate the local Sourcegraph-lite repo index for files, symbols, imports, status, and query.

Command:

```bash
npm run smoke:repo-index
```

Expected:

```text
SISO_REPO_INDEX_SMOKE_OK
```
