# Autopilot Fix Loop

Goal: validate the bounded repair controller for passing checks, failing checks, unsafe command blocking, failure summaries, and gathered context.

Command:

```bash
npm run smoke:autopilot-fix-loop
```

Expected:

```text
SISO_AUTOPILOT_FIX_LOOP_SMOKE_OK
```
