# Gather Context

Goal: validate the task-aware context gathering workflow that combines tool recommendation, ranked repo maps, search/read evidence, and related checks.

Command:

```bash
npm run smoke:gather-context
```

Expected:

```text
SISO_GATHER_CONTEXT_SMOKE_OK
```
