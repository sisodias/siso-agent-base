# Scenario: TUI Workbench

Goal: prove clean-room TUI demo surfaces render across supported modes/widths.

Run:

```bash
npm run smoke:tui-demo
```

Pass criteria:

- demo modes render
- narrow/medium/wide widths are covered
- output remains bounded and provider-free
