# Scenario: Benchmark Research Inbox

Goal: keep external benchmark/GitHub/Sourcegraph research normalized so it can become SISO benchmark tasks and build tickets.

Run:

```bash
npm run smoke:benchmark-research
```

Pass criteria:

- external benchmark map is valid
- each benchmark has category, research status, SISO use, measured dimensions, and local metrics
- research questions and local existing research notes exist
