# Scenario: Capability Registry

Goal: prove SISO's feature memory is structurally valid.

Run:

```bash
npm run smoke:capabilities
```

Pass criteria:

- no duplicate IDs
- valid statuses
- implemented/validated capabilities include implementation and validation metadata
